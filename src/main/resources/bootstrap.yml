
server:
  port: 8888

spring:
  profiles:
      active: composite
  application:
    name: config-app
  cloud:
    config:
      server:
        composite:
          - type: native
            search-locations: classpath:/config



